import React, { FC } from "react";
import Logo from "./Logo";
import Profile from "./Pofile";
const Admin = () => {
  return (
    <header>
      <Logo />
      <Profile />
    </header>
  );
};

export default Admin;
